exports.verify = (args, cb) => cb(true)

exports.problem = `
Creating Inline Styles

If you want to style a specific element there are several ways to approach it.

One way is 'inline' styles, this process is where you use the 'style' attribute, of
a given element or tag and save your html file.

    <h1 style="font-size: 4em;color: green;background-color: yellow">Hello World</h1>

We used the inline style feature to increase the font size by 4 times,
set the 
`
