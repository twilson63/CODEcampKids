exports.verify = (args, cb) => cb(true)

exports.problem `

rm - is a command that will remove or delete a specified file.

    rm [filename]

Will remove the file from the file system

Practice removing files, by removing the larry and moe files from the
previous problem then run:

    $ADVENTURE_COMMAND verify

`
