exports.problem = `

touch - the command that is used to create a file from the command-line.

To create a file in a directory simply call the touch command with the name
of the file as an argument.

    touch myfile

The touch command creates an empty file in the working directory.

Practice creating files using the the touch command - in your current working
directory create three files: larry, curly and moe then run:

  $ADVENTURE_COMMAND verify

`
