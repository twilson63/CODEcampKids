exports.verify = (args, cb) => cb(true)

exports.problem = `
Exercise 1

In this exercise, we will take a Mad Lib and convert it into an HTML Document.

create the Mad Lib using the run command:

    $ADVENTURE_COMMAND run

Open the file in your text editor and convert the first line to a 'h1' and wrap each
paragraph in the 'p' tags. Once complete run the verify command.

    $ADVENTURE_COMMAND verify


`
