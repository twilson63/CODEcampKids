exports.problem = `

git clone

The git clone command will pull a repository from the cloud and install it
on your computer. The repository can be in an 'url' format.

git clone https://gitlab.chscoderdojo.com/cyber/academy.git

type the above command to pull down the academy site.

then run verify:

    $ADVENTURE_COMMAND verify



`
